# List of basic metrics

Apply the Metrics schema a metrics object.

## Usage

``` r
schema_metrics(x)
```

## Arguments

- x:

  A list of metrics, with elements `numSpecs`, `numAPIs`, and
  `numEndpoints` (and likely several other elements).

## Value

A validated list of metrics.
